import { SvgJsDirective } from './svg-js.directive';

describe('SvgJsDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgJsDirective();
    expect(directive).toBeTruthy();
  });
});
